import classes from "./FeatureText.module.css";

const FeatureText = () => {
  return (
    <div className={classes.featureText}>
      <div className={classes.featureTextContent}>
        <h2 className={classes.featureTextContentTitle}>Features</h2>
      </div>
    </div>
  );
};

export default FeatureText;
